# 更新记录

## 版本 1.1.2

修复内容：

1. `SKILL.md` 明确要求先读取 `references/技能说明.md` 中的规则文件导航表。
2. `07_学习强度配置模板.md` 移除“错题专项模式 开启/关闭”配置项，避免把运行时行为误当预设开关。
3. `05_错题本模板.md` 默认行不再硬编码 `复习间隔 = 1天`，改为待填写。
4. `06_当前学习状态模板.md` 将“插问记录”改为未处理插问摘要，详细内容统一存 `08_插问停车场.md`。
5. 增加错题专项中用户主动“继续学新考点/退出错题专项”的处理规则。
6. 增加文件确认阶段回复 3 查看文件后的回流规则：查看后回到文件确认菜单，不直接进入学习强度选择。
7. 补充对应测试场景。

## 版本 1.1.1

修复内容：

1. 将学科和题型自动适配规则补入 `教学规则.md`，不再只存在于历史存档。
2. 更新覆盖检查规则，明确检查范围是 `技能说明.md` 加 4 个执行规则文件。
3. 同步 `使用说明.md`，补充规则文件拆分、间隔复习、错题专项、动态路线、优先级调整、四选项和插问停车场回收。
4. 明确四选项中选 4 的执行流程：默认 3 题、按当前题型生成、不给答案、完成后回到四选项。
5. 为自定义训练模式补充错题回顾插入策略的交互格式和示例。
6. 增加插问停车场回收规则：考点通关、错题专项结束或用户主动要求时检查。
7. 在 `09_考点通关记录模板.md` 增加“当前优先级”列。
8. 在典型例题处增加“来源：资料原文 / 自拟”标注。
9. 为 `04_学习进度表模板.md` 增加“下一步建议”示例值。

## 版本 1.1.0

优化内容：

1. 将臃肿的核心说明拆分为状态机、教学规则、文件工作流、训练配置四类规则文件。
2. 保留 `完整规则原文.md`，避免拆分时丢失原始约束。
3. 将 `04_学习进度表.md` 定义为当前状态表。
4. 将 `09_考点通关记录.md` 定义为只追加的历史快照。
5. 为 `05_错题本.md` 增加下次复习日期、已复习次数、复习间隔和复习状态。
6. 增加 1天 / 3天 / 7天 的错题间隔复习规则。
7. 增加两个新考点之间自动插入到期错题回顾的规则。
8. 增加“复习错题本”专项模式。
9. 增加按错题表现动态调整学习路线的规则。
10. 在文件确认阶段增加“回复 4 调整某个考点的优先级”。
11. 将文件更新说明模板改为按实际文件动态填写。
12. 补充空文件夹、扫描版 PDF、多门课混合、英文资料、等待答题时退出等边界测试。
13. 明确不加入时间压力模式，不要求考试日期，不按倒计时压缩路线。

## 版本 1.0.0

创建内容：

1. 创建通用交互式学习 Skill。
2. 支持任意课程资料读取。
3. 支持重点抽取和考点覆盖矩阵。
4. 支持训练强度选择。
5. 支持交互式讲解和练习。
6. 支持中途插问后回到学习流程。
7. 支持错题本和学习进度维护。
8. 支持考点通关后的四选项菜单。
9. 支持文件生成反馈。
10. 支持中文和阿拉伯数字命名规范。
11. 加入交互学习状态机。
12. 加入用户输入识别规则。
13. 加入断点续学规则。
14. 加入文件生成后暂停确认。
15. 加入回复前自检清单。
16. 加入防漂移规则。
17. 加入覆盖检查规则。
